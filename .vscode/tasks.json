{
    "tasks": [
        {
            "type": "shell",
            "label": "make cMatrice",
            "command": "cd ${workspaceFolder} && /usr/bin/make",
            "args": [
                
            ],
            "options": {
                "cwd": "/usr/bin"
            }
        }
    ],
    "version": "2.0.0"
}